# 如何將一段加密的HTML文本，在輸入正確密碼後，再解密並顯示出來



你想要的效果是：  
- 網頁源碼裡完全看不到真正的 HTML 內容（按 F12 也看不懂）  
- 只有輸入正確密碼後，才動態解密並顯示真正的頁面內容  
- 純前端、無需伺服器

以下是目前最安全、最簡單、最實用的完整範例（已測試可直接用）：

```html
<!DOCTYPE html>
<html lang="zh-TW">
<head>
<meta charset="UTF-8">
<title>密碼解鎖加密頁面</title>
<style>
    body {
        margin:0; height:100vh; background:#000; color:#fff;
        font-family:Arial; display:flex; justify-content:center; align-items:center;
        flex-direction:column;
    }
    #lockScreen {
        text-align:center;
    }
    #lockScreen input {
        padding:15px 20px; font-size:20px; width:300px; margin:20px;
        border:none; border-bottom:3px solid #0f0; background:transparent;
        color:white; text-align:center; outline:none;
    }
    #lockScreen button {
        padding:12px 30px; font-size:18px; background:#0f0; color:black;
        border:none; cursor:pointer; border-radius:6px;
    }
    #content { display:none; padding:40px; }
</style>
</head>
<body>

<!-- 鎖定畫面 -->
<div id="lockScreen">
    <h1>HelloWorld</h1>
    <p>輸入密碼以解鎖隱藏內容</p>
    <input type="password" id="password" placeholder="請輸入密碼" autocomplete="off">
    <br>
    <button onclick="unlock()">解鎖</button>
    <p id="msg" style="color:red; margin-top:15px;"></p>
</div>

<!-- 這裡是你要加密的真正內容（會被完全隱藏） -->
<div id="content">
    <h1>解鎖成功！歡迎管理員</h1>
    <p>這段內容在原始碼裡是加密的，沒密碼完全看不懂！</p>
    <hr>
    <p>你可以放任何東西：</p>
    <ul>
        <li>後台管理面板</li>
        <li>私人日記</li>
        <li>隱藏連結</li>
        <li>甚至整個管理系統</li>
    </ul>
    <button onclick="location.href='https://google.com'">範例：進入後台</button>
</div>

<!-- 引入 CryptoJS（AES 加密庫） -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/crypto-js/4.2.0/crypto-js.min.js"></script>

<script>
// 設定你的密碼（請改成自己的！）
const CORRECT_PASSWORD = "abbaaab";   // 改成你想要的密碼

// 這是你要隱藏的「真正內容」，已經被 AES 加密過
// 你只需要產生一次，之後永遠用這串亂碼即可
const ENCRYPTED_CONTENT = "U2FsdGVkX1+xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"; 
// ↑ 這串是加密後的亂碼（下面教你怎麼產生）

let decrypted = false;

function unlock() {
    const input = document.getElementById("password").value;
    const msg = document.getElementById("msg");

    if (!input) {
        msg.textContent = "請輸入密碼";
        return;
    }

    try {
        // 嘗試用輸入的密碼解密
        const bytes = CryptoJS.AES.decrypt(ENCRYPTED_CONTENT, input);
        const originalHTML = bytes.toString(CryptoJS.enc.Utf8);

        if (originalHTML && originalHTML.includes("<")) {  // 簡單判斷是不是 HTML
            // 解密成功 → 取代整個頁面內容
            document.body.innerHTML = originalHTML;
            decrypted = true;
        } else {
            throw new Error();
        }
    } catch (e) {
        msg.textContent = "密碼錯誤！";
        document.getElementById("password").value = "";
    }
}

// 可選：按 Enter 也能解鎖
document.getElementById("password").addEventListener("keypress", function(e) {
    if (e.key === "Enter") unlock();
});
</script>

</body>
</html>
```

### 如何產生你的加密內容（只要做一次）

打開瀏覽器 Console（F12），貼上這段並執行：

```js
const password = "abbaaab";  // 改成你的密碼
const realHTML = `<div style="padding:50px; text-align:center;">
    <h1 style="color:#0f0;">後門已開啟！</h1>
    <p>這是真正隱藏的內容，只有知道密碼的人看得到</p>
    <hr>
    <p><a href="https://你的後台.com" style="color:#0f0; font-size:20px;">點我進入管理後台</a></p>
</div>`;

const encrypted = CryptoJS.AES.encrypt(realHTML, password).toString();
console.log("你的加密字串（複製這段貼回 ENCRYPTED_CONTENT）：");
console.log(encrypted);
```

執行後會輸出一串亂碼（像 U2FsdGVkX1+...），把它貼回上面程式碼的 `ENCRYPTED_CONTENT` 變數即可！

### 優點
- 原始碼完全看不到真實內容（按 F12 只看到一串亂碼）
- 沒有密碼根本無法解密（AES-256 軍規等級）
- 支援任意 HTML（包含 script、style、按鈕、iframe 全都可以）
- 不需伺服器，單一 HTML 檔案就能用


```

<html><head></head><body>

<div id="outM">Hello</div>
<script src="https://cdnjs.cloudflare.com/ajax/libs/crypto-js/4.2.0/crypto-js.min.js"></script>

<script>

const password = "abb";  // 改成你的密碼

const realHTML = `hello`;
/* const realHTML = `<div style="padding:50px; text-align:center;">
    <h1 style="color:#0f0;">後門已開啟！</h1>
    <p>這是真正隱藏的內容，只有知道密碼的人看得到</p>
    <hr>
    <p><a href="https://你的後台.com" style="color:#0f0; font-size:20px;">點我進入管理後台</a></p>
</div>`;
*/

const encrypted = CryptoJS.AES.encrypt(realHTML, password).toString();
console.log("你的加密字串（複製這段貼回 ENCRYPTED_CONTENT）：");
console.log(encrypted);
document.getElementById("outM").innerHTML = encrypted ;

</script>
</body></html>
```


以下就是一個 **完全獨立的單一 HTML 檔案**，  
打開就能用，什麼都不用安裝：

- 輸入你想要的密碼  
- 貼上你要加密的任意 HTML（可以包含 `<script>`、`<style>`、按鈕、連結…全部都行）  
- 點「產生加密字串」→ 立刻得到一串亂碼  
- 把這串亂碼貼到前一個「解鎖頁面」的 `ENCRYPTED_CONTENT` 變數裡就大功告成！

```html
<!DOCTYPE html>
<html lang="zh-TW">
<head>
<meta charset="UTF-8">
<title>AES 加密工具 - 產生加密後的 HTML</title>
<style>
    body {
        font-family: Arial, sans-serif;
        max-width: 900px;
        margin: 40px auto;
        padding: 20px;
        background: #111;
        color: #eee;
        line-height: 1.6;
    }
    h1 { color: #0f0; text-align:center; }
    textarea, input {
        width: 100%;
        padding: 12px;
        margin: 10px 0;
        font-size: 16px;
        background: #222;
        color: #0f0;
        border: 1px solid #0f0;
        border-radius: 6px;
        box-sizing: border-box;
    }
    textarea { height: 240px; font-family: Consolas, monospace; }
    button {
        padding: 14px 30px;
        font-size: 18px;
        background: #0f0;
        color: #000;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        margin: 20px 0;
    }
    button:hover { background: #0c0; }
    #result {
        background: #000;
        padding: 15px;
        border: 2px dashed #0f0;
        word-break: break-all;
        min-height: 80px;
        margin-top: 20px;
    }
    .copy {
        float: right;
        padding: 8px 16px;
        background: #333;
        color: #0f0;
        border: none;
        cursor: pointer;
        font-size: 14px;
    }
</style>
</head>
<body>

<h1>AES 加密工具</h1>
<p>用來把「真正想隱藏的 HTML」加密成亂碼，貼到解鎖頁面使用</p>

<label>1. 輸入密碼（記住它！解鎖時要用同一組）</label>
<input type="password" id="pwd" placeholder="例如：abbaaab" value="abbaaab">

<label>2. 貼上你要加密的完整 HTML 內容（可包含 script、style、任何標籤）</label>
<textarea id="plain" placeholder='<h1 style="color:red;">這是隱藏內容</h1><p>只有知道密碼的人看得到！</p><script>alert("後門開啟")</script>'></textarea>

<button onclick="encrypt()">產生加密字串</button>

<label>3. 加密結果（點右邊「複製」按鈕即可一鍵複製）</label>
<div id="result">結果會顯示在這裡…</div>
<button class="copy" onclick="copyResult()" style="display:none;" id="copyBtn">複製</button>

<!-- 引入 CryptoJS -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/crypto-js/4.2.0/crypto-js.min.js"></script>

<script>
function encrypt() {
    const password = document.getElementById("pwd").value.trim();
    const plainHTML = document.getElementById("plain").value;

    if (!password) {
        alert("請先輸入密碼！");
        return;
    }
    if (!plainHTML) {
        alert("請貼上要加密的 HTML 內容！");
        return;
    }

    try {
        const encrypted = CryptoJS.AES.encrypt(plainHTML, password).toString();
        document.getElementById("result").textContent = encrypted;
        document.getElementById("copyBtn").style.display = "inline";
    } catch (e) {
        alert("加密失敗！請檢查內容是否正常");
    }
}

function copyResult() {
    const text = document.getElementById("result").textContent;
    navigator.clipboard.writeText(text).then(() => {
        alert("已複製到剪貼簿！");
    });
}

// 按 Enter 也能觸發加密（在密碼欄或內容欄）
document.getElementById("pwd").addEventListener("keypress", e => { if (e.key === "Enter") encrypt(); });
document.getElementById("plain").addEventListener("keypress", e => { if (e.key === "Enter" && e.ctrlKey) encrypt(); });
</script>

<p style="text-align:center; margin-top:50px; color:#888;">
    使用方式：<br>
    1→2→點「產生加密字串」→複製結果<br>
    然後貼到你的解鎖頁面的 <code>ENCRYPTED_CONTENT</code> 變數裡即可！
</p>

</body>
</html>
```

### 使用步驟（超簡單）

1. 把上面整段程式碼存成 `encrypt-tool.html`
2. 用瀏覽器打開
3. 輸入你想要的密碼（例如 `abbaaab`）
4. 貼上你要隱藏的完整 HTML（可以直接從其他檔案複製整段 `<div>...</div>`）
5. 點「產生加密字串」
6. 點右邊「複製」按鈕
7. 把複製的亂碼貼到前面「解鎖頁面」的這一行：

```js
const ENCRYPTED_CONTENT = "剛剛複製的那串亂碼";
```



以下是一個 **單一 HTML 檔案**，功能完整、超好看、直接打開就能用：

### 功能一覽
- 輸入密碼  
- 貼上你之前用工具產生的「加密亂碼」  
- 點擊「解密並顯示」 → 立即在頁面中顯示真正的 HTML 內容（支援 `<script>`、`<style>`、按鈕、圖片…全部都可以執行！）  
- 同時也是測試工具：你可以隨時驗證「密碼對不對、加密內容有沒有壞掉」

```html
<!DOCTYPE html>
<html lang="zh-TW">
<head>
<meta charset="UTF-8">
<title>AES 解密測試 & 預覽工具</title>
<style>
    body {
        margin:0; background:#000; color:#0f9; font-family:Arial, sans-serif;
        display:flex; flex-direction:column; height:100vh;
    }
    header {
        background:#111; padding:20px; text-align:center; border-bottom:3px solid #0f9;
    }
    h1 { margin:0; color:#0f9; }
    main {
        flex:1; padding:30px; overflow:auto;
    }
    textarea, input {
        width:100%; padding:15px; margin:15px 0; background:#111; color:#0f9;
        border:2px solid #0f9; border-radius:8px; font-size:16px; font-family:Consolas, monospace;
    }
    textarea { height:180px; resize:vertical; }
    button {
        padding:14px 32px; font-size:18px; background:#0f9; color:#000;
        border:none; border-radius:8px; cursor:pointer; margin:10px 5px;
    }
    button:hover { background:#0c8; }
    #preview {
        margin-top:30px; padding:25px; background:#001; border:2px dashed #0f9;
        border-radius:10px; min-height:200px; display:none;
    }
    .info { color:#888; font-size:14px; text-align:center; margin:20px; }
</style>
</head>
<body>

<header>
    <h1>AES 解密預覽工具</h1>
    <p>輸入密碼 + 加密亂碼 → 立即看到真正內容（含 script 也會執行）</p>
</header>

<main>
    <label>1. 輸入解密密碼</label>
    <input type="password" id="pwd" placeholder="例如：abbaaab" value="abbaaab">

    <label>2. 貼上加密後的亂碼（ENCRYPTED_CONTENT）</label>
    <textarea id="encrypted" placeholder="U2FsdGVkX1+xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"></textarea>

    <div style="text-align:center;">
        <button onclick="decryptAndShow()">解密並顯示內容</button>
        <button onclick="document.getElementById('preview').style.display='none'">清除預覽區</button>
    </div>

    <div id="preview">
        <h2 style="color:#0f9;">解密成功！以下是真實內容：</h2>
        <div id="realContent"></div>
    </div>

    <div class="info">
        小技巧：解密後如果看到按鈕、表單、甚至跳 alert，都代表 100% 成功！
    </div>
</main>

<!-- 引入 CryptoJS -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/crypto-js/4.2.0/crypto-js.min.js"></script>

<script>
function decryptAndShow() {
    const password = document.getElementById("pwd").value.trim();
    const encryptedText = document.getElementById("encrypted").value.trim();
    const preview = document.getElementById("preview");
    const container = document.getElementById("realContent");

    if (!password || !encryptedText) {
        alert("密碼和加密內容都要填喔！");
        return;
    }

    try {
        const bytes = CryptoJS.AES.decrypt(encryptedText, password);
        const decryptedHTML = bytes.toString(CryptoJS.enc.Utf8);

        if (!decryptedHTML) throw new Error("解密失敗");

        // 顯示並執行真正的 HTML
        container.innerHTML = decryptedHTML;
        preview.style.display = "block";
        
        // 強制重新渲染 script（CryptoJS 解密後的 <script> 預設不會執行）
        container.querySelectorAll("script").forEach(old => {
            const script = document.createElement("script");
            script.textContent = old.textContent;
            if (old.src) script.src = old.src;
            old.parentNode.replaceChild(script, old);
        });

    } catch (e) {
        alert("密碼錯誤或內容損壞！\n無法解密，請檢查密碼是否正確");
        preview.style.display = "none";
    }
}

// 支援 Enter 鍵快速解密
document.getElementById("encrypted").addEventListener("keydown", e => {
    if (e.ctrlKey && e.key === "Enter") decryptAndShow();
});
</script>

</body>
</html>
```

### 使用方式（超簡單）

1. 把上面程式碼存成 `decrypt-test.html`
2. 用瀏覽器直接打開
3. 輸入你設定的密碼（例如 `abbaaab`）
4. 貼上之前用「加密工具」產生的那一長串亂碼
5. 點「解密並顯示內容」→ 瞬間看到真正的畫面！

這個頁面同時就是你最強的測試工具：  
只要能成功顯示＋所有按鈕、alert、跳轉都能正常運作，就代表你的加密完全沒問題，可以放心用在正式的隱藏頁面上了！


